#include <arpa/inet.h>  
#include "server.h" 

int main(void){

	//ServerPolicy *pol= new TCPServerPolicy();
	//Client *cli_ = new Client(pol);
	Server *ser_=new Server();
	//ser_->createSocket();

return 0;
}
